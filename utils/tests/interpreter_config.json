{
  "type": "interpreter-config",
  "configurations": [
    {
      "settings": [
        {
          "driverOptions": {
            "host": "localhost",
            "port": 4445
          },
          "browserOptions": {
            "browserName": "firefox",
            "build": "${TRAVIS_BUILD_NUMBER}",
            "tags": [
              "${TRAVIS_BRANCH}"
            ],
            "username": "${SAUCE_USERNAME}",
            "accessKey": "${SAUCE_ACCESS_KEY}",
            "tunnel-identifier": "${TRAVIS_JOB_NUMBER}",
            "public":"public"
          }
        },
        {
          "driverOptions": {
            "host": "localhost",
            "port": 4445
          },
          "browserOptions": {
            "browserName": "chrome",
            "build": "${TRAVIS_BUILD_NUMBER}",
            "tags": [
              "${TRAVIS_BRANCH}"
            ],
            "username": "${SAUCE_USERNAME}",
            "accessKey": "${SAUCE_ACCESS_KEY}",
            "tunnel-identifier": "${TRAVIS_JOB_NUMBER}",
            "public":"public"
          }
        }
      ],
      "scripts": [
        "utils/tests/selenium/*"
      ]
    }
  ]
}
